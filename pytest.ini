[pytest]
# Pytest configuration for Economic Dashboard API

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output settings
addopts = 
    -v
    --tb=short
    --strict-markers
    --ignore=tests/test_financial_sector_features.py
    --ignore=tests/test_insider_quick.py
    --ignore=tests/test_locally.py
    --ignore=tests/test_margin_risk.py
    --ignore=tests/test_refresh_setup.py
    --ignore=tests/test_recession_model.py


# Async mode for pytest-asyncio
asyncio_mode = auto

# Markers
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (may require database)
    slow: Slow tests (ML training, large datasets)
    api: API endpoint tests
    database: Database layer tests
    ml: Machine learning pipeline tests

# Warning filters
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Logging
log_cli = true
log_cli_level = WARNING
