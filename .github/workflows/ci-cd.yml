name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        python -m pytest tests/ -v --tb=short

    - name: Check code quality
      run: |
        python -m py_compile app.py
        python -m py_compile modules/data_loader.py
        python -m py_compile pages/1_Economic_Indicators_Deep_Dive.py
        python -m py_compile pages/2_Financial_Markets_Deep_Dive.py

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
    - uses: actions/checkout@v4

    - name: Deploy to Streamlit Cloud
      run: |
        # Placeholder for Streamlit Cloud deployment
        # In a real scenario, you would use Streamlit's deployment action
        echo "Deployment would happen here"
        # streamlit deploy command or similar